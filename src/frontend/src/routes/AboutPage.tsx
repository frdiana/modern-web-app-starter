export function AboutPage() {
  return (
    <section className="about-page">
      <p className="eyebrow">About this starter</p>
      <h1>Built to be replaced.</h1>
      <p className="intro">
        Keep the routing and API boundary, then shape the interface around the
        product you are building.
      </p>
    </section>
  )
}